# smol
tiny key-value store

## Usage

```sh
PORT=<port> ./master
```

```sh
PORT=<port> ./volume
```

## TODO
- rocksdb
- port to rust if python isn't fast enough
- fancy frontend in next + shadcn ui